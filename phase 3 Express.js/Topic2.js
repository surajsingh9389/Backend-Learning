// Continue Phase 3 – JSON & Routes

// Express does NOT parse JSON automatically

// app.use(express.json());
// This middleware:

// Reads incoming request body

// Parses JSON

// Attaches it to req.body