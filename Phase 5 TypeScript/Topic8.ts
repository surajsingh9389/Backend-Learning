// 1. Helmet: A security package that automatically sets various HTTP response headers to protect your app from well-known web vulnerabilities like XSS (Cross-Site Scripting) and clickjacking.

// 2. Morgan: An HTTP request logger that records details about incoming requests (like the URL, method, and response time) to your console or a file, which is helpful for debugging and monitoring traffic.

// 3. Express-rate-limit: A middleware used to limit repeated requests from the same IP address, helping to prevent brute-force attacks and DoS (Denial of Service) abuse.

// 4. CORS (Cross-Origin Resource Sharing): A package that configures your server to allow or restrict requests coming from different domains (origins), ensuring that only authorised websites can access your API.